import React from 'react';
import languages from '../../languages';
import './PrivateEvents.scss';

function PrivateEvents() {
  return (
    <section id={languages.eng.events}>
      <h2>Private Events</h2>
    </section>
  );
}

export default PrivateEvents;
